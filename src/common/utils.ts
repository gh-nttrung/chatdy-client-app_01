// import jwt from "jsonwebtoken";
// import { AuthData } from "./types";

// export const getAuthData = (token: string): AuthData | null => {
//   try {
//     // // const secretKey = `${process.env.SECRET_KEY}`;
//     var data = null;
//     // jwt.verify(token, secretKey, (_, decoded) => {
//     //   if (decoded) {
//     //     data = decoded as AuthData;
//     //   }
//     // });
//     return data;
//   } catch (error) {
//     throw error;
//   }
// };
